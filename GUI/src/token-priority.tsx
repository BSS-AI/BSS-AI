import React from 'react';
import ReactDOM from 'react-dom/client';
import TokenPriorityWindow from './components/TokenPriorityWindow';
import './index.css';
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TokenPriorityWindow />
  </React.StrictMode>,
);
